API_KEY="xxx"
model_name="gpt-4o-mini"
API_BASE="xxx"
JUDGE_MODEL="gpt-4.1"
JUDGE_API_KEY="xxx"
JUDGE_BASE_URL="xxx"


PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=XXXX
PG_DBNAME=rwkv-eval

# Evaluation dashboard (FastAPI + React/Vite SPA).
#   Backend:  python -m src.space.api           (serves API + built SPA on :7860)
#   Frontend: cd frontend && pnpm dev           (Vite dev server on :5173, proxies /api -> :7860)
# Optional overrides for the leaderboard score index location:
# RWKV_SPACE_SCORE_INDEX=/path/to/score_index.jsonl
# RWKV_SPACE_RESULTS_DIR=/path/to/results
# Optional: require a bearer token on the /api/admin/* scheduler control routes.
# RWKV_ADMIN_API_KEY=change-me
