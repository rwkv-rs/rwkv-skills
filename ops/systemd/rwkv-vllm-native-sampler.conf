[Service]
# Rapid sampling currently treats greedy requests as a fatal incompatibility.
# Reserved interactive endpoints must accept temperature=0 without killing the
# whole engine, so use the native sampler for these services.
Environment=VLLM_USE_RAPID_SAMPLER=0
