# Lexical Chunk Router

Compatibility path for the standalone `lexical_chunk_router` package.

Use:

```python
from lexical_chunk_router import LongDocConfig, ToolRouterConfig, compact_text, route_tools
```
