# RWKV Agent Eval Plugin

Compatibility path for the standalone `rwkv_agent_eval_plugin` package.

Use:

```python
from rwkv_agent_eval_plugin import agent_plugin_config_from_sources, route_agent_prompt_inputs
```
