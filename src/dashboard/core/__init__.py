"""Framework-agnostic leaderboard logic (no FastAPI, no HTML).

Pure data/score modules shared by the web layer: score loading & normalisation,
metric extraction, model selection/lineage, pivot-table assembly, chart series,
domain helpers, tokeniser display, and the score-index reader/writer.
"""
